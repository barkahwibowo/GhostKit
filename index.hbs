{{! index.hbs}}
{{!< default}}

			<div class="uk-grid uk-margin-top" data-uk-grid-margin="">
				<div class="uk-width-medium-1-1">
					<div class="uk-vertical-align uk-text-center" style="background-image: url({{@blog.cover}}); height: 400px;">
						<div class="uk-vertical-align-middle uk-width-1-2">
							<h1 class="uk-heading-large">{{@blog.title}}</h1>
							<p class="uk-text-large">{{@blog.description}}</p>
						</div>
					</div>
				</div>
			</div>

			<section id="latestpost">
			{{> loop}}
			</section>
			<section id="pagination">
			<hr class="uk-grid-divider">

			{{pagination}}
			</section>